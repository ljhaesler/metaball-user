{
  "name": "metaball-user",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "serve": "esbuild index.js --bundle --outfile=public/build.js --format=esm --watch --serve --servedir=public",
    "build": "esbuild index.js --bundle --platform=browser --format=esm --minify --outfile=public/build.js"
  },
  "dependencies": {
    "pixi.js": "^8.18.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
